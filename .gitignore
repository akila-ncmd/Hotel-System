/.phpunit.cache
/node_modules
# /public/build is deliberately committed: home.blade.php loads the WebGL hero
# and gallery from it, and without the bundle a fresh clone silently falls back
# to CSS. Run `npm run build` after touching resources/js so the committed
# assets stay in step with the source.
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Claude Code
/.claude
CLAUDE.md

# Database dumps — contain real personal data (names, emails, phone numbers, password hashes)
*.sql

# Generated reports
/storage/app/reports

# Stray shell-history artifacts
/cd
/php
